/* Retired.
 *
 * Static GitHub Pages cannot enforce authentication. Cutset's public pages
 * render sanitized read-only projections only. Real authentication and launch
 * authority belong in the private backend.
 */
